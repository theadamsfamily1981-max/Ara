# Ara Avatar Dependencies
# Install with: pip install -r requirements-avatar.txt

# Core server
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
requests>=2.28.0

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0

# ASR (pick one or more)
# faster-whisper>=0.10.0  # Recommended - requires CUDA
# vosk>=0.3.45            # Good offline alternative
# SpeechRecognition>=3.10.0  # Google Cloud (requires internet)

# TTS (pick one or more)
# piper-tts>=1.2.0        # Recommended - fast local TTS
# TTS>=0.22.0             # Coqui - high quality but slower
# gTTS>=2.3.0             # Google (requires internet)

# Note: espeak is usually pre-installed on Linux
# If not: sudo apt install espeak

# GTK UI (install via apt)
# sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1

# Quick start (minimal):
# pip install fastapi uvicorn pydantic requests sounddevice numpy scipy
# sudo apt install espeak python3-gi gir1.2-gtk-4.0 gir1.2-adw-1

# Full voice (recommended):
# pip install fastapi uvicorn pydantic requests sounddevice numpy scipy faster-whisper piper-tts
