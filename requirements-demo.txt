# Ara Demo - Minimal requirements
# For 16GB GPU (RTX 5060) or CPU-only mode

# Core only - no heavy ML
numpy>=1.21.0

# Optional: Formal verification (uncomment if you want PGU)
# z3-solver>=4.12.0

# That's it! The cognitive loop runs without heavy deps.
# Full requirements.txt needed only for production/training.
